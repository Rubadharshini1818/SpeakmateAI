services:
  - type: web_service
    name: SpeakmateAI Backend
    env: java
    plan: free
    branch: main
    buildCommand: cd backend && ./mvnw clean package -DskipTests
    startCommand: cd backend && java -jar target/backend-0.0.1-SNAPSHOT.jar
